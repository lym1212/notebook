```shell
git init
git add .    添加到暂存区
git commit -m "xxx"   提交到本地仓库
git branch   查看分支
git checkout master   切换分支
git checkout -b xxx   创建并切换分支
git push -u origin xxx   推送分支到远程仓库
git merge xxx    合并分支
git push
git branch -M master
git push -u origin master   push到master分支

git status  查看文件状态
```
